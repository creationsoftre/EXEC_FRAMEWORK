-- CAPO: Optional helper for scripts that want one import for every EXEC bridge.
return {
  Hud = require 'bridge.hud',
  Appearance = require 'bridge.appearance',
  Identity = require 'bridge.identity',
  Inventory = require 'bridge.inventory',
  Killfeed = require 'bridge.killfeed',
  Notify = require 'bridge.notify',
  Spawn = require 'bridge.spawn',
  Target = require 'bridge.target',
  Voice = require 'bridge.voice',
}
